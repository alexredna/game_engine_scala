import scala.language.implicitConversions

object Test
{
	/*class Food()
	case class Cheese(i: Int) extends Food {
		def add(n: Int) {
			println("Cheese " + n)
		}
	}
	case class Bread(i: Int) extends Food {
		def add(n: Int) {
			println("Bread " + n)
		}
	}

	implicit def int2Cheese(i: Int) = {
		if (i < 0)
			return Cheese(0)
		Cheese(i)
	}
	implicit def int2Bread(i: Int) = {
		if (i >= 0)
			return Bread(0)
		Bread(i)
	}

	def main(args: Array[String])
	{
		2 add 3
		-2 add 4
	}*/
}